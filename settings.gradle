rootProject.name = 'etri.services'

['utils','event', 'async','planet'].each {
	include ":$it"
	project(":$it").projectDir = file("../../github/$it")
}
['camus.model'].each {
	include ":$it"
	project(":$it").projectDir = file("../$it")
}