rootProject.name = 'etri.services'

ext.common_dir = '../../common'
['utils', 'utilsx', 'event', 'async','planet'].each {
	include ":$it"
	project(":$it").projectDir = file("$common_dir/$it")
}
['camus.model'].each {
	include ":$it"
	project(":$it").projectDir = file("../$it")
}
